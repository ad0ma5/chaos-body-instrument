

we kindly expect your answer to be pure code no commens inside or outside code.
output is single file html with minimal css and inline vanilla js.

we have a tone.js project and we want to add configuration capabilities. we need to be able to preselect media files/samples with test button to test it loaded and then 5 instances of it for 5 samples and functionality to save and autoload from localstorage posibility to add url or load local file from harddrive

Debug.ts:95 The AudioContext is "suspended". Invoke Tone.start() from a user action to start the audio.
Debug.ts:95 The AudioContext is "suspended". Invoke Tone.start() from a user action to start the audio.
